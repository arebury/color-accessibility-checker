services:
  - type: web
    name: color-accessibility-checker
    runtime: node
    region: oregon
    plan: free
    branch: main
    rootDir: mcp-server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: "18"
