services:
  - type: web
    name: color-accessibility-checker
    runtime: node
    plan: free
    region: oregon
    rootDir: .
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
    autoDeploy: true
